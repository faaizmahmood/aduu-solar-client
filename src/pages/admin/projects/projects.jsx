import styles from './projects.module.scss'

const Projects = () => {
    return (
        <>
            <section className={`${styles.projects}`}>
                Admin Projects
            </section>
        </>
    )
}

export default Projects