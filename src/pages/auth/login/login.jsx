import styles from './login.module.scss'

const Login = () => {
    return (
        <>

            <section className={`${styles.login}`}>
                Login
            </section>

        </>
    )
}

export default Login