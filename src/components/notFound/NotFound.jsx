import styles from './NotFound.module.scss'

const NotFound = () => {
  return (
    <>
        <section className={`${styles.notFound}`}>
        Not Found - 404
        </section>
    </>
  )
}

export default NotFound